# Optional. The Vite dev server already proxies /api to http://localhost:8000
# (see vite.config.ts), so you do NOT need this for local development.
# Only set it when the frontend is served separately from the backend
# (e.g. a production build pointing at a deployed API).
VITE_API_URL=http://localhost:8000
